# --- Web/Vite layer ---
node_modules/
dist/
dist-ssr/
.env
.env.production
.env.*.local
*.local

# --- Native Android layer (inside android/) ---
# These patterns have no leading slash, so git matches them at any depth —
# they cover android/build/, android/app/build/, android/.gradle/, etc. automatically
build/
.gradle/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# --- Editor / OS ---
.DS_Store
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- Logs ---
*.log
npm-debug.log*
